# Pipeline Profiles

Profiles define which modules run together for a given workflow.

- `p30_*`: segmentation-stage profiles
- `p60_*`: analysis-stage profiles
- `p90_*`: end-to-end study profiles

Use these as templates and copy to project-specific runtime configs.
