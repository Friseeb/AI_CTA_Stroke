name: cta_centerline_mps
channels:
  - conda-forge
dependencies:
  - python=3.10
  - numpy<2
  - scipy>=1.10
  - nibabel>=5.2
  - networkx>=3.2
  - scikit-image>=0.22
  - tqdm>=4.66
  - pip
  - pip:
      - torch==2.2.2
      - torchvision==0.17.2
      # Optional: Julia/Metal EDT backend (distance_transforms). Install manually if needed.
      # - distance-transforms
